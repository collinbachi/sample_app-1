<%= form_for(current_user.leader_member_relationships.build(leader_id: @group.id)) do |f| %>
  <div><%= f.hidden_field :member_id %></div>
  <%= f.submit "Join", class: "btn btn-small btn-primary" %>
<% end %>